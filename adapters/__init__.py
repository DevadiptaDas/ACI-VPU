"""ACI adapters - per-consumer connectors that sit on top of the universal
cognition primitives. The LLM connector is just ONE adapter; filesystem, OS,
sensor, and app adapters use the exact same primitives."""
