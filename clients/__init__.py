"""ACI clients - thin connectors over the open HTTP interface.

These are NOT special adapters. They are ordinary consumers that any developer
in any language could write, all talking to the same ACI service. The Python
ones here use only urllib (stdlib).
"""
