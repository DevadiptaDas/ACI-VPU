"""ACI-VPU 60-second demo (repo copy). After `pip install aci-vpu` you can also
just run the `aci-demo` command -- it runs exactly this. Kept here so it works
straight from a `git clone` too."""
from aci.demo import main

if __name__ == "__main__":
    main()
